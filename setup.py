from setuptools import setup

setup()

# A minimal setup file, so develpment mode can be used with pip versions < 21.2 
# (see https://setuptools.pypa.io/en/latest/userguide/quickstart.html#development-mode)